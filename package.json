{
  "name": "remitstream",
  "version": "1.0.0",
  "description": "Recurring USDT0 payroll streams on Conflux eSpace",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network espaceTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network espaceMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "2.0.8",
    "@nomicfoundation/hardhat-ethers": "3.0.8",
    "@nomicfoundation/hardhat-network-helpers": "1.0.12",
    "@nomicfoundation/hardhat-verify": "2.0.12",
    "chai": "4.5.0",
    "ethers": "6.13.5",
    "hardhat": "2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.0.2",
    "dotenv": "16.4.5"
  }
}
